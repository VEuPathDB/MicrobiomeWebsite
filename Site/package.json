{
  "dependencies": {
    "icon-font-generator": "^2.1.8"
  },
  "scripts": {
    "install": "mkdir -p dist && icon-font-generator icons/* -o dist -n mbio-icons -p mbio --normalize --center --csstp ./templates/icons-css.hbs"
  }
}
